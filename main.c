#include "main.h"
/**
 * main - entry point.
 * @ac: number of argument in the argv array
 * @argv: array of string
 * Return: return 0 when successful
 */
int main(int ac, char **argv)
{
	(void)ac;
	run_shell(argv);
	return (0);
}
